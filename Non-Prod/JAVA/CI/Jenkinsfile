pipeline {
    agent { label 'agent any' }
    parameters {
          choice (
              name: 'mode', choices: ['deploy', 'stop', 'start', 'restart'], description: 'deployment description' )
    }
}

